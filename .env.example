# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require
POSTGRES_URL=postgresql://user:password@host:5432/database?sslmode=require

# Auth (JWT)
JWT_SECRET=tu_jwt_secret_32_caracteres_minimo

# OpenAI API (para transcripción con Whisper y análisis con GPT)
# https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# AssemblyAI API (para archivos grandes >25MB, hasta 5GB)
# https://www.assemblyai.com/dashboard
# OPCIONAL: Si no está configurado, archivos >25MB fallarán
ASSEMBLYAI_API_KEY=

# Vercel Blob Storage (para almacenar archivos)
# https://vercel.com/dashboard > Storage > Blob
BLOB_READ_WRITE_TOKEN=vercel_blob_...

# Inngest (para procesamiento asíncrono)
# https://app.inngest.com
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# Cron Jobs (para limpieza automática)
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=

# Resend (para notificaciones por email - opcional)
# https://resend.com/api-keys
RESEND_API_KEY=
ADMIN_EMAIL=admin@annalogica.eu

# Cloudinary (DEPRECADO - ahora se usa Vercel Blob)
# NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
# NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=

# AI Assistant (DEPRECADO - no usado actualmente)
# NEXT_PUBLIC_ENABLE_AI_ASSISTANT=false
# GEMINI_API_KEY=

# Instrucciones:
# 1. Copia este archivo como .env.local
# 2. Reemplaza todos los valores con tus credenciales reales
# 3. Variables REQUERIDAS:
#    - OPENAI_API_KEY (obligatorio)
#    - POSTGRES_URL (obligatorio)
#    - BLOB_READ_WRITE_TOKEN (obligatorio)
#    - JWT_SECRET (obligatorio)
#    - INNGEST_EVENT_KEY, INNGEST_SIGNING_KEY (obligatorio para producción)
# 4. Variables OPCIONALES:
#    - ASSEMBLYAI_API_KEY (para archivos >25MB)
#    - CRON_SECRET (para cron jobs)
#    - RESEND_API_KEY (para notificaciones email)
